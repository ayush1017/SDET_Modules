// See https://aka.ms/new-console-template for more information
using System;
namespace ConsoleApp1
{
    class Program
    {
        //A variable is a named storage location in memory that can hold a value.
        //it is stronly typed, meaning that the type of value it can hold is defined at compile time.
        //Console WriteLine is a method that writes a line of text to the console.
        //Console.Write is a method that writes text to the console without a new line.


        static void Main(string[] args)
        {
            Console.WriteLine("Hello , World!");
            int number;
            number=Convert.ToInt32 (Console.ReadLine());
            
        }
    }
}
